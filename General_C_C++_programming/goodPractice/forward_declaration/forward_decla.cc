#include<iostream>
//look at other two .cc file, notice the para of the functions and their names
void forward_funct(float a);//this is forward declaration

int main()
{
    forward_funct(3);
    return 0;
}